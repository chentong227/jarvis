# =============================================================================
# Jarvis Personal Butler — Development Dependencies
# =============================================================================
# 在 requirements.txt 之上追加测试 / lint 工具
# 用法： pip install -r requirements-dev.txt
# =============================================================================

-r requirements.txt

# === 测试 ===
pytest==8.4.2

# === Lint / 格式化 ===
# ruff 替代 flake8 + isort + black，对 nerve.py 这种巨型文件友好（速度极快）
ruff>=0.6,<1.0

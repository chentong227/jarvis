---
description: Cursor mirror of docs/JARVIS_PYTHON_STYLE.md. Source of truth is the markdown.
globs: jarvis_*.py
alwaysApply: false
---

# Jarvis Python Style (mirror)

> **真理源**: `docs/JARVIS_PYTHON_STYLE.md` — 必须读那里, 本文件是 Cursor 触发器.
> 非 Cursor agent (Windsurf / Codex / Claude Code) 走 `AGENTS.md §2` 必读表的引用读 doc.

改 `jarvis_*.py` 之前 Grep `docs/JARVIS_PYTHON_STYLE.md`, 看:

- §1 **Imports safety net** — `from __future__ import annotations` + `import numpy as np  # if any np.* used` + json/os/sys/re/time/threading/collections 全预 import 带 `# noqa: F401`
- §2 **Marker 三处一致** — `[<commit-marker> / <ISO date>]` 三处同步 (代码注释 / commit / TODO)
- §3 **bg_log vs print 选择树** — daemon 用 bg_log, 启动 print OK, dialog 渲染路径禁 print
- §4 **Forbidden anti-patterns** — `from jarvis_nerve import *` / `time.sleep(0.0X)` 主线程 busy-loop / raw `sqlite3.connect("memory_pool/...")` / `logging.info` / 硬编码 `_<X>_PATTERNS`
- §5 **Type hints** — 新代码必须, 老代码可选
- §6 **准则 6.5 vocab 持久化三件套** — `memory_pool/<x>_vocab.json` + `scripts/<x>_dump.py` CLI + py `_SEED_<X>_PATTERNS` fallback + `get_<x>_patterns()` mtime cache
- §7 **Sensitive data 访问** — `sir_profile.json` 读 OK 但 NEVER print to chat reply; `memory_pool/*.db` 走 Hippocampus API

## 变更协议

**只改 `docs/JARVIS_PYTHON_STYLE.md`**, 不在本 mdc 加 doc 没有的规则。本 mdc outline 跟着 doc 章节顺序同步。

违反: 任何"只在 mdc 写, doc 没有"的规则 → 非 Cursor agent 看不到 → 跨 IDE 可携带性破。
